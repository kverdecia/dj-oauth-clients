============
Installation
============

At the command line::

    $ easy_install dj-oauth-clients

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv dj-oauth-clients
    $ pip install dj-oauth-clients
